<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Material 3 theme. DayNight so it follows the in-app dark/light toggle. -->
    <style name="Theme.PinBoard" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/brand_container</item>
        <item name="colorOnPrimaryContainer">@color/brand_container_text</item>
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

</resources>